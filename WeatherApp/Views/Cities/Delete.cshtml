@model WeatherApp.Models.City

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


    <div  style=" background: rgba(0,0,0,0.7); padding-left:20px">
        <h1 style="color:#FFD500;">Delete</h1>

        <h4 class="text-white">Are you sure you want to delete this City?</h4>
        
        <hr />
        <dl class="row">


            <dt class="col-sm-2 text-white text-bold">
                @Html.DisplayNameFor(model => model.name)
            </dt>
            <dd class="col-sm-10 text-white text-bold">
                @Html.DisplayFor(model => model.name)
            </dd>
            <dt class="col-sm-2 text-white text-bold">
                @Html.DisplayNameFor(model => model.country)
            </dt>
            <dd class="col-sm-10 text-white text-bold">
                @Html.DisplayFor(model => model.country)
            </dd>
        </dl>

        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <input type="submit" value="Delete" class="btn btn-danger" /> |
            <a class="btn btn-dark" asp-action="Index">Back to List</a>
        </form>
    </div>
